[project]
name = "pyxivdata"
version = "0.1.0.7.30"
description = "FFXIV data extraction package"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"
